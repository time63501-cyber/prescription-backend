[phases.setup]
nixPkgs = ["tesseract5", "tesseract5.langs.eng", "libGL", "libglib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn run:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
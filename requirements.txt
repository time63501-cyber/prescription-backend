# Web framework
flask>=3.0.0
werkzeug>=3.0.0

# Production Server (Required for Railway)
gunicorn>=20.1.0

# OCR
pytesseract>=0.3.13

# Image processing  — use headless build on Railway (no display required)
opencv-python-headless>=4.9.0
Pillow>=10.3.0
numpy>=1.26.0